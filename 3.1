#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
    float x;
    float y;
    
    
    cout << "wprowadź dwie liczby" << endl;
    cin >> x;
    cin >> y;
    
    
    float a = x + y;
    float b = x - y;
    float c = x * y;
    float d = x / y;
    
    cout << "suma Wynosi:" << setprecision(2) << a << endl ;
    cout << "roznica Wynosi:" << setprecision(2) << b << endl ;
    cout << "iloczyn Wynosi:" << setprecision(2) << c << endl ;
    cout << "iloraz Wynosi:" << setprecision(2) << d << endl ;
    
    

    return 0;
}
